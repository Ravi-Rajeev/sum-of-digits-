#include <stdio.h>
#include <math.h>
int main(){
int n,x,sum=0,rem;
printf("Enter the number:\n");
scanf("%d",&n);
while(n!=0)
    {
        rem=n%10;
        sum+=rem;
        n=n/10;
        }
        printf("The sum is %d",sum);
}
